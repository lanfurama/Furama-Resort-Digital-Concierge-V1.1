# Database Configuration
# ⚠️ IMPORTANT: Replace these with your actual database credentials
# Never commit .env file to git!
DB_HOST=your_database_host
DB_PORT=your_database_port
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# SSL Configuration (optional)
# Set to 'true' to enable SSL, 'false' to disable, or leave empty for auto
DB_SSL=

# Gemini API Key (for frontend - must have VITE_ prefix)
# ⚠️ IMPORTANT: Replace with your actual Gemini API key
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Server Port (optional, defaults to 3000)
PORT=3000

# HTTPS Configuration
# Set to 'true' to enable HTTPS, 'false' or leave empty for HTTP
ENABLE_HTTPS=false

# HTTPS Port (optional, defaults to PORT value - 3000)
# Nếu không set, sẽ sử dụng PORT cho HTTPS
# HTTPS_PORT=3000

# HTTPS Redirect (optional)
# Set to 'true' to redirect HTTP to HTTPS
HTTPS_REDIRECT=false

# SSL Certificate Paths (optional, defaults to certs/server.key and certs/server.crt)
# SSL_KEY_PATH=certs/server.key
# SSL_CERT_PATH=certs/server.crt

# Node Environment
# Use 'production' for production, 'development' for development
NODE_ENV=production

# Logging Configuration
# Log level: 'debug', 'info', 'warn', 'error' (default: 'info' for production, 'debug' for development)
LOG_LEVEL=info
# Frontend log level (for browser console)
VITE_LOG_LEVEL=info

# Email Configuration (for notifications)
# Gmail SMTP settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password
DEFAULT_FROM_EMAIL=your_email@gmail.com

